#ifndef SHARED_NATIVE_H
#define SHARED_NATIVE_H

#include <stdint.h>

/** Adds two integers. Replace with the real thing. */
int64_t shared_native_add(int64_t a, int64_t b);

#endif /* SHARED_NATIVE_H */
