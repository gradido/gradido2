# generated by c-cpp-zig-build
build/
.zig-cache/
.zig-native/
compile_commands.json

# `zig build exe` and the codegen when it is run by hand rather than by build.zig
zig-out/
gen/

# The preview language is per developer, not per repository (.mjmlconfig.js)
.preview-locale
