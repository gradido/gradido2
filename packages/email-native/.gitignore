# generated by c-cpp-zig-build
build/
.zig-cache/
.zig-native/
compile_commands.json

# `zig build exe`; the codegen run by hand writes into build/tools, which build/ covers
zig-out/

# The preview language is per developer, not per repository (.mjmlconfig.js)
.preview-locale
